{
  "work_dir": "/app/workdir",
  "artifact_dir": "/app/artifacts",
  "verbose": true,
  "amo_instances": {
    "amo_server": {
      "$if": "ENV == 'prod'",
      "then": "https://addons.mozilla.org",
      "else": "https://addons.allizom.org"
    },
    "jwt_user": {
      "$eval": "JWT_USER"
    },
    "jwt_secret": {
      "$eval": "JWT_SECRET"
    }
  }
}
